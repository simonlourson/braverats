import { CardArrayPipe } from './card-array.pipe';

describe('CardArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new CardArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
